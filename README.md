Instructions to run:

should be very easy

I used a conda env running python 3.12.13 but both of those things should be flexible

If you are using a new environment the pip install cell should be run

after that nothing is required, just running each cell in order. (one cell does take a while tho, around 5 minutes)
